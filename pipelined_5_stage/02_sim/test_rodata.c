int main (void) {
    const char *message = "Hello, this is a read-only string!\n";

    while (1) {
        // Infinite loop to keep the program running
    }

    return 0;  // Return from main (never actually reached in a bare-metal program)
}
